      function applyFilter(value) {
            const tableWidget = PF('statusTable');
            tableWidget.cfg.filterMap = {}; // reset filters
            if (value) {
                tableWidget.cfg.filterMap['type'] = value;
            }
            tableWidget.filter();
        }
